# Tasks

## 1. roadmap/ directory and documentation

- [ ] 1.1 Create `roadmap/` at the repo root with a `.gitkeep` file so the empty directory is tracked.
- [ ] 1.2 Document the roadmap convention in `OCTOPUS.md`: the `roadmap/<slug>.md` location, the frontmatter fields (title, status, added), the status values and their meaning, that items are human- and agent-editable (not autocoder-owned, no gate enforcement), and that they are never automatically implemented. Note that an agent running a discuss or audit session should check `roadmap/` for relevant prior thinking.
